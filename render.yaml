services:
  - type: web
    name: openclaw
    plan: free
    region: singapore
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    healthCheckPath: /
    numInstances: 1
